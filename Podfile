# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FavoriteRecipes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FavoriteRecipes
  pod 'Alamofire', '~> 5.2'
  pod 'SDWebImage', '~> 5.0'

  target 'FavoriteRecipesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FavoriteRecipesUITests' do
    # Pods for testing
  end

end
